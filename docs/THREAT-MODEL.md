# 威胁模型：SDK 匿名采集

## 接受的风险
- SDK 接口为匿名采集模式，不验证客户端身份
- 任何持有有效 app_id 的客户端均可写入数据
- 恶意写入可能导致数据污染和存储膨胀

## 已实施的缓解措施
1. IP 级速率限制（10次/秒/IP）
2. Nginx client_max_body_size 1MB
3. 写入失败时不返回假成功
4. 运维可通过 dashboard 发现异常写入模式

## 待实施的增强（后续迭代）
1. app_id + HMAC 签名鉴权
2. 单 app 配额
3. 异常写入告警
